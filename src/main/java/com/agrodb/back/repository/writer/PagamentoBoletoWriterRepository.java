package com.agrodb.back.repository.writer;

import com.agrodb.back.models.PagamentoBoleto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PagamentoBoletoWriterRepository extends JpaRepository<PagamentoBoleto, Integer> {
}
